# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## プロジェクト概要

不動産関連アプリケーション(realestate-app)。現時点ではリポジトリは初期化されたばかりで、技術スタックやディレクトリ構成は未確定。プロジェクトが具体化してきたら、このセクションおよび「コマンド」「アーキテクチャ」セクションを実態に合わせて更新すること。

## Git運用ルール

- **コードを変更するたびに、都度コミットしてGitHubへプッシュする。** 変更をローカルに溜め込まず、意味のある変更(ファイル追加・修正・削除)を行ったらその都度コミット&プッシュする。
- コミットメッセージは変更内容が分かるように簡潔に書く。
- プッシュ前に `git status` / `git diff` で差分を確認し、意図しないファイル(認証情報、`.env`、ビルド成果物など)が含まれていないか確認する。
- force push (`git push --force`)、`git reset --hard`、履歴の書き換えなど破壊的な操作は、ユーザーの明示的な許可なく行わない。
- リモートリポジトリ(GitHub)が未設定の場合は、プッシュ前にユーザーに確認する。

## コマンド

技術スタックが未確定のため、ビルド・Lint・テストコマンドは未記載。フレームワーク(例: Next.js, Prisma など)や言語構成が決まり次第、以下を追記すること:

- 依存関係のインストールコマンド
- 開発サーバー起動コマンド
- ビルドコマンド
- Lint / フォーマットコマンド
- テスト実行コマンド(全体実行・単一テスト実行の両方)

## アーキテクチャ

コードベースがまだ存在しないため、アーキテクチャ情報は未記載。主要なディレクトリ構成やモジュール間の関係が固まった段階で、複数ファイルを跨ぐ設計判断(例: フロントエンド/バックエンドの分割、データフロー、認証方式など)を中心に記載すること。
